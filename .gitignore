# Personal, machine-local Claude Code settings (permissions etc.) — never commit.
# The shared, cross-device config lives in .claude/settings.json (Rule 22).
.claude/settings.local.json

# macOS Finder metadata
.DS_Store
